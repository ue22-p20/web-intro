// premier pas
// on définit une fonction qui change la couleur de fond
function set_body_background(color) {
    document.querySelector("body").style.backgroundColor = color;
}

// on peut mettre ce code ici si on veut
// et il se peut même que ça marche
// **MAIS** c'est accidentel
// sinon on peut le taper dans la console...
set_body_background("darkgrey");
